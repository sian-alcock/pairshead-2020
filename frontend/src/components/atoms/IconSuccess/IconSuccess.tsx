import React, { ReactElement } from 'react';
import Icon from '../Icons/Icons';

import './iconSuccess.scss';

export const IconSuccess = () => {
  return (
    <i className="icon-success"><Icon icon={'success-tick'}/></i>
  )
}