import React, { ReactElement } from 'react';
import Icon from '../Icons/Icons';

import './iconFail.scss';

export const IconFail = () => {
  return (
    <i className="icon-fail"><Icon icon={'fail-cross'}/></i>
  )
}

